{
  "version": 2,
  "buildCommand": "cp prisma/schema.production.prisma prisma/schema.prisma && npx prisma migrate deploy && npm run build",
  "builds": [
    {
      "src": "api/index.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/api/index.ts"
    }
  ]
}
